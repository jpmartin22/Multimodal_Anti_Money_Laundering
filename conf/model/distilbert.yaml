# @package _global_
model:
  name: "distilbert"
  model_name: "distilbert-base-uncased"
  max_length: 64
  num_labels: 2
  weight_decay: 0.01

metrics:
  target_threshold: 0.80
  primary_key: "auc_pr"
